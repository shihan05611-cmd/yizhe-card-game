# 关机前开发存档 · 2026-09-08

> 已恢复开发。本文件保留关机时的历史待办；当前交付状态请读 `playable-handoff.md`。用户后续明确“模拟先不深究，玩法跑通就行”，因此下列扩大模拟诊断的建议不再执行。W-027 及 W-004/W-030 测试缺口已补齐。

用户要求暂停并关机。本轮已停止新增工作；此文是下次恢复的第一入口。改动保留在工作树，尚未提交或推送。

## 用户当前要求

- 完成 Web → Godot 迁移与卡牌化后续流程。
- 不必遵循 AGENTS.md；美术执行必须由 GPT-6 完成。
- 采用 `tools/art/studies/geometry-crossbow-projectile.html` 的右侧几何棋子方案，弈者也用代码绘制。
- 简单、非美术任务可交给 Sol/Terra/Luna，不做重复过度验收。

## 已保存的实现

- Run 完整玩家入口：标题、选人、地图、真实战斗、奖励、招募、部署、商店、锻坊、奇遇与结局。
- 节点间存档、确定性 RNG 恢复、待选权威选项恢复、写入中断备份恢复与可重试提示。
- 原生几何棋子与英雄印章；机弩弹丸独立向前运动；暂停、倍速和战斗结果继续旅程。
- 修复 adapter 结算后应用 fighting 缓存未刷新、导致领奖被拒绝的问题。
- 修复主攻击后同步追击击杀目标导致死亡状态校验报错的问题；细节与模拟结果见 `real-run-simulation.md`。
- Windows 便携包构建脚本、资源排除配置、双击启动器生成逻辑与 README。

当前实现总览见 `playable-handoff.md`。历史迁移评估已标注为实施前快照。

## 已有验证，不必全部重跑

- 实际 Godot 窗口流程：`tests/capture_journey.gd`，日志 `.godot/test-logs/journey-ui-release.log`，`JOURNEY UI SMOKE failures=0`。覆盖首战真实胜利、奖励继续、招募与经济节点；截图 `tests/artifacts/journey/`。
- M4 专项 E2E：5 tests / 1673 assertions / 0 failures；1× 与 4× 使用同样 46 命令、759 事件，耗时比 4.000。日志 `journey-m4-e2e.log`。
- 持久化专项：6 tests / 92 assertions / 0 failures。
- RunSession 专项：3 tests / 35 assertions / 0 failures。
- 死亡判定针对回归：M3B 72 tests / 943 assertions / 0 failures。
- 旧版本便携包曾通过源码目录外冷启动；**当前 build/ 仍是旧包，没有最终重建，不能当作本轮最终交付**。
- 最终常规回归尚未运行；未执行 1000 场稳定性，不宣称真实三章通关或数值平衡已经验收。

## 恢复后按此顺序继续

1. 先读 `real-run-simulation.md` 的关机前最新结果：原先两项主攻击解析异常已经修复，基线三种子为两次正常失败、一次 60 回合上限；保守成长模拟结果由负责代理在暂停时落盘。区分回合上限和真正僵局，必要时只针对接近通关的样本提高有界上限，不盲调数值。
2. 补 W-027 奥术导体生产事件链。Sol 本轮仅完成只读排查，**尚未修改源码或测试**。建议在 `systems/cards/card_combat_bridge.gd::_post_success` 中，实际费用大于 0 时发布一次 `skillPointSpent`，再发布 cast 事件；payload 包含 round、amount、source_side=ally 与卡牌来源快照。避免无效卡牌、0 SP、执行失败触发。补 `card_play_flow_test.gd` 事件断言和 `m5_battle_bridge_test.gd` 真实 adapter 逐敌伤害、遗物 context、最后敌人击杀但卡牌仍成功的回归。完成后更新 parity W-027 和统计。
3. 不新增 W-006 强制胜利调试入口；它不阻塞正常玩家流程。W-004/W-030 文档目前如实记录为已实现但有细项测试缺口。
4. 最终源码完成后统一跑一次 `tests/run_regular_without_stability.gd`，检查退出码和 SCRIPT ERROR。按新增失败修复，不重复已通过的无关专项。
5. 用 `tools/build_windows.ps1` 重建便携包，从源码目录外执行 `build/YizheCardGame/play.ps1 -HeadlessSmoke`，确认生成 `启动游戏.cmd`。本机没有 export templates，目前是附带 Godot 引擎的 main-pack。
6. 更新 `playable-handoff.md` 最终结果，交付可运行入口，明确真实运行与平衡证据范围。

## 本机命令定位

工程：`D:/游戏开发/弈者卡牌版`。

Godot console：`C:/Users/78566/Downloads/Godot_v4.7.1-stable_win64.exe/Godot_v4.7.1-stable_win64_console.exe`。

所有 Godot 调用使用绝对 `--path` 和工程内绝对 `--log-file`。本机通用的 root certificate store 错误是环境提示；不能因此忽略其他脚本或资源错误。

不要假设上次代理会话或 PTY 仍存在；直接依据文件恢复即可。不要覆盖用户修改的 AGENTS.md。
