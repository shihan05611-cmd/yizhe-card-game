# ART-4 整合、响应式验证与视觉证据

## 目标

整合 ART-1～3，生成 `tests/artifacts/art1` 的八张证据并完成行为回归。

## 允许范围

- ART-1～3 已触及的 `ui/`、`scenes/` 与 InputMap 的必要整合修正
- `tests/`、`tests/artifacts/art1/`
- `docs/art-layout-handoff.md` 与 parity/验收记录

## 禁止范围

- 不修改战斗规则、Controller/VM/事件合同、队列结算或 M5/M6。
- 不推进第二期风格工作，不因主观色彩/字体偏好返工。
- 不运行 1000 场回归。

## 依赖

- ART-1、ART-2、ART-3 各自聚焦测试通过。

## 实现要求

1. 统一处理跨批 NodePath、容器 flags、z 序与输入传播。
2. 图形捕获生成：默认日志隐藏、日志打开、7 手牌、0 手牌、hover/drag、结果页、1600×900、1280×720。
3. 视觉只修复缺失资源、不可读、明显遮挡/溢出、错误状态；不做像素级风格裁决。
4. 建立 ART 聚焦 runner，保留 M4 聚焦与常规非稳定性回归。

## 完成条件

- 八张 PNG 均存在、尺寸正确、状态与文件名一致。
- 1200×700、1280×720、1600×900 无明显溢出/裁切/空洞；0 手牌无框，7 手牌安全可读。
- ART 聚焦、M4 聚焦、图形 smoke、常规非稳定性回归通过；1000 场明确省略。
- 交接文档记录接受项、保留项、测试与可调美术参数。

