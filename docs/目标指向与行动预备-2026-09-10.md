# 目标指向与行动预备

状态：已实现并验收。

- 斩杀拖拽显示指向线、候选描边、锁定准星和目标文字，降低卡牌遮挡；无效位置明确提示退回。
- 棋子行动指定我方存活弈子，赋予可叠层的普通 Buff；在下一回合获得额外行动，不能在当前回合提前触发，不占附魔槽。
- 验证拖拽目标与落点一致、多格目标、取消清理，以及行动预备的叠层、延迟生效、消耗与状态显示。

验收：常规回归 451 项测试、14992 条断言，0 失败；真实卡牌到 Buff 标签及指向界面专项 10 项测试、156 条断言，0 失败。保留既有常规 runner 对千场稳定性与独立自动战斗套件的排除；本次另验证自动目标与排队流程 9 项测试、68 条断言。已检查 `tests/artifacts/card_target_preview.png` 与 `tests/artifacts/action_buff_preview.png` 的画面。
