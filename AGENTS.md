# 《弈者》卡牌版项目协作规则
本项目的中枢角色，应尽量让确定性或者基础性的任务应交给terra或Luna执行，节省成本与自身上下文；重复模块的任务应优先复用已有子agent；
代码生成美术的实现，则需要由gpt6完成（思考强度用中等即可）



## 5. 当前冻结的玩法边界

当前玩法说明的权威锚点是 [docs/玩法介绍-当前版本.md](docs/玩法介绍-当前版本.md)。后续玩法改动必须同步更新该文档、相关测试与本节的简要边界；用户新决定可以修订锚点，不受历史冻结文案限制。

- 玩家采用卡牌系统；不恢复逐弈者行动额度、旧自由技装备槽和玩家大招决策面板。敌方继续使用既有旧式选技与能量规则。
- 自由技以允许重复的 ID 多重集持有，每个副本独立；主动专属技随上阵弈者加入牌库，骑士被动不生成专属卡但计入 N。
- 上阵弈者解锁其初始主动专属与后续专属的奖励/商店候选池；额外获得专属副本独立记录在 `exclusive_card_ids`，不混入自由技。初始专属每名主动弈者仍只有默认一张，大招不入获取候选池。乘隙改为沉戈后续专属；影狩初始潜影成功使用后回手，回合末正常弃置。
- 首版手牌上限 7，每回合新增抽 `2+N`；能量满立即清零并生成大招卡，满手时入抽牌堆顶。具体规则以 M3 交接和相关测试为准。
- `basicDamage` 保留底层定义/handler，但不进入玩家卡牌目录与装配。
- Run 开局无神通，从零费用初始自由技中取两种不同技能，各一张；精确部署保存于 `hero_deployment_slots`。
- Run 新局默认六槽为 `null, shield, null, assassin, crossbow, banner`，仅四名弟子：甲卒在前排 2 号，其他三兵种在后排 4–6 号。地图可拖拽换位或替换已部署弟子的兵种，每兵种默认两份库存；不能用换兵种向空位增员。旧六满存档保留原阵容并兼容迁移。M4 直开路径仍为 `default×6`，不得混为一谈。
- `charge`、`assault`、`sacrifice` 已迁入休眠模块；未经后续明确产品决定，不接入 Run、GameRoot、Controller、UI 或输入。两件神通进化遗物继续从实际候选池过滤。


以上是当前基线，不禁止用户在新任务中明确修改这些规则。
